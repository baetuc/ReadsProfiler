#include <string>
#include <vector>
#include <climits>
#include <iostream>
#include <stdio.h>
#pragma once

using namespace std;

class Utility {
public:
    static string getStringForNumber(int number);
    static string getStringForFloat(float number);
    static vector<string> splitString(string source, char delimiter, int maxParts);
    static const char* concatenateStrings(const char* first, const char* second, const char* third);
};

